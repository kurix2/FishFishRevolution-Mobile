using UnityEngine;
using System.Collections;

public class Reel: MonoBehaviour {
	
	public NetworkView nView;
	
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
       /* if (Input.GetKey(KeyCode.R))
        {
            nView.RPC("reel", RPCMode.All);
        }*/
	}
}
